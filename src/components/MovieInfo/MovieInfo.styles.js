import styled from "styled-components";
import { IMAGE_BASE_URL, BACKDROPSIZE } from '../../config';

export const Wrapper = styled.div``;

export const Content = styled.div``;

export const Text = styled.div``;